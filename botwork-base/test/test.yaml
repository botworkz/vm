type: test

image: "@artifact://botwork-base/botwork-base.qcow2"

uploads:
- src: "@goss"
  dest: /usr/local/bin/goss
  mode: 0755
- src: "@://botwork-base/test/goss.yaml"
  dest: /tmp/goss.yaml

steps:
- name: base-goss
  run: |
    goss -g /tmp/goss.yaml validate

diagnostics_units:
- ssh
