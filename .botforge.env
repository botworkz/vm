
export HOST_UID="${HOST_UID:-$(id -u)}"
export HOST_GID="${HOST_GID:-$(id -g)}"
export HOST_KVM_GID="${HOST_KVM_GID:-$(getent group kvm | cut -d: -f3)}"

botforge() {
    docker compose run --remove-orphans botforge "$@"
}
